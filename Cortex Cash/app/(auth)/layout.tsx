/**
 * Layout para páginas de autenticação
 * Agent CORE: Sistema de Autenticação
 *
 * Layout simplificado sem sidebar/header para login e cadastro
 */

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
