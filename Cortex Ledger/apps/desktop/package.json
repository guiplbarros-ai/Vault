{
  "name": "cortex-ledger-desktop",
  "version": "0.1.0",
  "description": "Cortex Ledger macOS Application Launcher",
  "scripts": {
    "create": "./create-mac-app.sh",
    "install-app": "cp -r 'Cortex Ledger.app' /Applications/",
    "build": "npm run create && npm run install-app"
  }
}
